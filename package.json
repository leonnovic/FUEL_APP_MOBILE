{
  "name": "fuel-app-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && ELECTRON_SKIP_BINARY_DOWNLOAD=1 npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
